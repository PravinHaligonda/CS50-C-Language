#include <stdio.h>
#include <cs50.h>

int main(void)
{
    int arr[] = {100, 50, 200, 500, 2, 1, 15};

    int n = get_int("Search: ");
    for (int i = 0; i < sizeof(arr); i++)
    {
        if ()
    }
}