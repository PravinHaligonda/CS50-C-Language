#include <stdio.h>
#include <cs50.h>
#include <string.h>

int main(int argc, string argv[])
{

    int count = (argc);
    for (int i = 0; i < count; i++)
    {
        printf("%i\n", i);
        printf("%s\n", argv[i]);
    }
    // printf(argv[])
    // if (argc >= 2)
    // {
    //     printf("Hello, %s\n", argv[count]);
    // }
    // else
    // {
    //     printf("Hello, world");
    //     return 0;
    // }
}